spring:
  application:
    name: ai-healthcare
  datasource:
    url: jdbc:mysql://localhost:3306/healthcare?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: Cjstktkddn1~~
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: healthcare-secret-key-must-be-at-least-32-characters-long
  expiration: 86400000

server:
  port: 8081